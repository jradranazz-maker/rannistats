{
  "_comment": "Address of the RanniStats push Worker. After `wrangler deploy` prints your Worker URL, paste it here as \"api\" and commit this file. The app reads it at runtime, so changing it does not need an index.html rebuild. Leave it as-is and notifications simply stay off.",
  "api": "PASTE_YOUR_WORKER_URL_HERE"
}
