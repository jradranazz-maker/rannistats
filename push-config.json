{
  "_comment": "Where the notification server lives. Read at runtime, so the address can change with a 300-byte upload instead of rebuilding index.html. Deployed 2026-09-19; verified answering /key with a 65-byte P-256 VAPID key.",
  "api": "https://rannistats-push.jradranazz.workers.dev"
}